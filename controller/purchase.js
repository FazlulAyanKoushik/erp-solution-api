// Require Main Modules..
const {validationResult} = require('express-validator');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
// Require Post Schema from Model..
const Purchase = require('../models/purchase');

exports.addPurchase = async (req, res, next) => {
    const errors = validationResult(req);

    if (!errors.isEmpty()) {
        const error = new Error('Input Validation Error! Please complete required information.');
        error.statusCode = 422;
        error.data = errors.array();
        next(error)
        return;
    }

    const data = req.body;
    console.log(data)
     
    const purchase = new Purchase(data);

    try {
        const response = await purchase.save();
        res.status(200).json({
            response,
            message: 'Purchase Added Successfully!'
        });
    } catch (err) {
        if (!err.statusCode) {
            err.statusCode = 500;
            err.message = 'Something went wrong on database operation!'
        }
        next(err);
    }
}

exports.getAllPurchases = async (req, res, next) => {
    const errors = validationResult(req);

    if (!errors.isEmpty()) {
        const error = new Error('Input Validation Error! Please complete required information.');
        error.statusCode = 422;
        error.data = errors.array();
        next(error)
        return;
    }

    const allPurchase = await Purchase.find();

    try {
        res.status(200).json({
            data: allPurchase,
            count: allPurchase.length
        });
    } catch (err) {
        if (!err.statusCode) {
            err.statusCode = 500;
            err.message = 'Something went wrong on database operation!'
        }
        next(err);
    }
}

exports.getPurchaseByPurchaseId = async (req, res, next) => {
    const errors = validationResult(req);

    if (!errors.isEmpty()) {
        const error = new Error('Input Validation Error! Please complete required information.');
        error.statusCode = 422;
        error.data = errors.array();
        next(error)
        return;
    }

    const purchaseId = req.params.purchaseId;
    const purchase = await Purchase.findOne({_id: purchaseId});

    try {
        res.status(200).json({
            data: purchase,
        });
    } catch (err) {
        console.log(err)
        if (!err.statusCode) {
            err.statusCode = 500;
            err.message = 'Something went wrong on database operation!'
        }
        next(err);
    }
}

exports.getSinglePurchaseBySlug = async (req, res, next) => {
    console.log(req.params.slug);
    const slug = req.params.slug;
    try {
        const query = {slug: slug};
        const data = await Purchase.findOne(query)

        console.log(data);

        res.status(200).json({
            data: data,
            message: 'Purchase fetch Successfully!'
        });
    } catch (err) {
        if (!err.statusCode) {
            err.statusCode = 500;
            err.message = 'Something went wrong on database operation!'
        }
        next(err);
    }
}

exports.editPurchaseData = async (req, res, next) => {

    const updatedData = req.body;

    try {
        await Purchase.updateOne({_id: updatedData._id}, {$set: updatedData})
        res.status(200).json({
            message: 'Purchase Updated Successfully!'
        });

    } catch (err) {
        console.log(err);
        if (!err.statusCode) {
            err.statusCode = 500;
            err.message = 'Something went wrong on database operation!'
        }
        next(err);
    }
}

exports.deletePurchaseByPurchaseId = async (req, res, next) => {

    const purchaseId = req.params.purchaseId;
    await purchase.deleteOne({_id: purchaseId});

    try {
        res.status(200).json({
            message: 'Purchase Deleted Successfully',
        });
    } catch (err) {
        if (!err.statusCode) {
            err.statusCode = 500;
            err.message = 'Something went wrong on database operation!'
        }
        next(err);
    }
}